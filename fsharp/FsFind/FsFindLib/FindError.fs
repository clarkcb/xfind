namespace FsFindLib

module FindError =
    let StartpathNotDefined = "Startpath not defined"
    let StartpathNotFound = "Startpath not found"
    let InvalidRangeMinDepthMaxDepth = "Invalid range for mindepth and maxdepth"
    let InvalidRangeMinLastModMaxLastMod = "Invalid range for minlastmod and maxlastmod"
    let InvalidRangeMinSizeMaxSize = "Invalid range for minsize and maxsize"
    let StartpathNotMatchFindSettings = "Startpath does not match find settings"

;;
